# WenStarOS 维护优化手册

> **日期**: 2026-07-28 | **问题编号**: P3-1 → P3-5 | **类型**: 维护文档

---

## P3-1: 配置依赖图

### 配置文件清单

| 文件 | 职责 | 被谁引用 |
|:---|:---|:---|
| `src/config/MemoryConfig.ts` | 三库记忆阈值/衰减/晋升参数 | MemoryAssessor, VaultManager, WorkingMemory |
| `src/config/M3Config.ts` | 钙化阈值/场景系数/五级闸门 | PerceptionAnalyzer, FiveStageGate, WorkingMemory |
| `src/config/TemporalConfig.ts` | 时空引擎参数 | SleepTimeConsolidator, HippocampusRhythmCoordinator |
| `src/config/family-graph-migration.ts` | FG 迁移脚本 | FamilyGraph initialize |
| `src/config/app-identity.ts` | AI/用户名称 + 农历 + 关键字 + 百家姓 | chat.ts, EntityCandidateGrader, GarbageEntityGuard |
| `src/common/const/llm-config.ts` | LLM 参数 | DeepSeekLLMProvider |
| `src/m4/MemoryConfig.ts` | 记忆检索参数 | MemoryRetriever, MemoryInjector |
| `src/m6/M6Config.ts` | 人格演化参数 | M6Orchestrator |
| `src/config/ConfigService.ts` | 运行时开关 (WS_DISABLE_*) | 全局 |

### 配置依赖图

```
MemoryConfig ← MemoryAssessor, VaultManager
     ↓
M3Config ← WorkingMemory, FiveStageGate
     ↓
M6Config ← M6Orchestrator
     ↓
app-identity ← chat.ts, EntityCandidateGrader
     ↓
ConfigService ← 全局开关 (LLM/PFC/Observability/检索)
```

---

## P3-2: 模块导览

### 目录结构 (新成员阅读路径)

```
src/
├── m1/          ① DNA编码 — 语义/实体/场景编码
├── m2/          ② 记忆存储 — SQLiteAdapter/schema/math
├── m3/          ③ 感知分析 — 24D向量/情绪标签/五级闸门
├── m4/          ④ 知识融合 — FG/MemoryRetriever/MemoryInjector/搜索引擎
│   └── household/  FamilyGraph + 周边(DossierService/GarbageEntityGuard/RelationResolver)
├── m5/          ⑤ 表达生成 — LLM Provider/persona/prompts/PromptAssembler
├── m6/          ⑥ 自我模型 — 人格/偏好/自传叙事/边界
├── m7/          ⑦ 梦境引擎 — 离线巩固/归纳/冲突整理
├── m8/          ⑧ 年轮引擎 — 关键事件锚定
├── m9/          ⑨ 工作记忆 — 短时缓冲/毕业
├── app/          应用层
│   ├── chat/      ChatPolicy/CognitivePipeline
│   ├── entity/    实体管理(EntityOwnershipResolver/UUIDHealth/CandidateGrader)
│   ├── role/      角色系统(RoleplayIsolationGuard/RoleplayRuleRegistry)
│   ├── knowledge/ KnowledgeEngine/FtsSearch
│   ├── alignment/ FG分支/向量对齐
│   └── vault/     MemoryAssessor/VaultManager/BlackDiamondGate
├── engine/       引擎层
│   └── tianquan/  天权引擎(prefrontal/temporal/heart/knowledge/bus)
├── webui/        Web层
│   ├── chat.ts   ⚠️ 核心编排文件 (2490行, 持续拆分中)
│   ├── server.ts ⚠️ 服务器 (2802行, 持续拆分中)
│   └── chat/     拆分模块(retrieval/persistence/CognitivePipeline/...)
├── config/       配置层
└── __tests__/    测试层
```

### 新成员推荐阅读顺序

```
1. 文档: docs/WenStarOS系统评价与改善路线图.md (全局概览)
2. 代码: src/m1 → m2 → m3 → m4 (按流水线方向)
3. 架构: docs/模块语义边界分析_PFC-M6-M3-EngineContext.md
4. 风险: docs/TS与Python双系统边界定义.md
```

---

## P3-3: 大文件拆分记录

| 文件 | 当前行数 | 已拆出 | 剩余可拆 |
|:---|:--:|:---|:---|
| `chat.ts` | 2490 | retrieval(200行), persistence(80行), CognitivePipeline(58行), PromptAssembler(211行), ChatPolicy(123行) | PFC组装段(~150行), M6注入段(~80行), 知识路由(~60行) |
| `server.ts` | 2802 | observability-routes(1100行), chat-routes, memory-routes, brain-routes, vault-routes | 启动初始化(~300行), 备份引擎(~100行) |
| `FamilyGraph.ts` | 5899 | DossierService, GarbageEntityGuard, RelationResolver | UUIDService, Dossier读写, 迁移脚本 |
| `SQLiteAdapter.ts` | 1662 | ConversationDB(独立类) | 回填逻辑(~80行), 查询方法(~200行) |

### 拆分原则

- 每次只拆一个模块，编译+测试后再拆下一个
- 新模块与旧文件并行运行一段时间，验证无回归后删除旧代码
- 拆出时不改变行为逻辑，纯搬迁

---

## P3-4: 历史兼容代码退场计划

| 模块 | 状态 | 替代方案 | 删除里程碑 |
|:---|:---|:---|:---|
| `src/m2/KnowledgeBase.ts` | **deprecated** | `KnowledgeEngine` 直接使用 | 8 处调用方全部迁移后删除 |
| `legacy buildRoleplayRules()` | 兼容中 | `RoleplayRuleRegistry + structured` | 行为测试覆盖后删除 |
| `relation_to_user` 直接读路径 | 兼容中 | `RelationResolver.resolveRelationToUser()` | grep 无业务读取后告警 |
| `_detectSpeakerUUID()` | **已删除** ✅ | `EntityOwnershipResolver` | — |
| `retrieval-stage 旧黑钻LIKE检索` | **已替换** ✅ | `UnifiedSearchEngine` | — |
| `finalKnowledgeText 线性拼接` | **并行中** | `PromptAssembler` (9/12块迁移) | 全部12块迁移后删除旧拼接链 |
| `_bdVecCache` 黑钻向量缓存 | **已删除** ✅ | `VectorReranker` 替代 | — |

---

## P3-5: 术语统一

| 术语 | 别名 / 易混淆 | 标准用法 |
|:---|:---|:---|
| **实体** | entity, person, character, 角色 | `entity` — 数据概念；`角色` — 扮演概念 |
| **UUID** | TXS-ID, uuid, belong_entity_uuid | `TXS-ID` (格式) / `belong_entity_uuid` (列名) |
| **记忆** | memory, 金库, 砂金, 黑钻 | `memories` (表) / `金库` (语义层) / `黑钻` (black_diamond) |
| **会晤** | entity_meeting, meeting, 实体会话 | `entity_meeting` (ChatMode kind) |
| **角色扮演** | roleplay, rp, 扮演 | `roleplay` (ChatMode kind) |
| **人格** | persona, personality, M6, 自我模型 | `M6` (模块) / `persona` (PromptBlock type) / `自我模型` (概念) |
| **钙化** | calcium, calcium_score, calcium_level | `calcium_score` (0-10 连续值) / `calcium_level` (0-3 离散值) |
| **门控** | gate, guard, policy, PFC | `PFC` (前额叶模块) / `ChatPolicy` (会话权限) / `guard` (PromptBlock safety) |
| **编排** | orchestrate, pipeline, assemble | `CognitivePipeline` (M4→M5) / `PromptAssembler` (块组装) / `retrieval-stage` (检索) |
