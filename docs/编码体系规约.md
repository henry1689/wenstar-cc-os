# WenStar 三体系统 · 编码体系规约 V1.0

> 版本：V1.0 ｜ 日期：2026-08-24 ｜ 范围：wenstar-cc（太虚 webui）+ wenstar_os（三体域）
> 目的：梳理全部编码体系的作用、生成者、消费方、生命周期与处置决策，消除编码混乱。
> 依据：2026-08-24 编码体系审计（代码调用链 + 数据库填充实况）。

---

## 一、为什么有这么多编码（背景）

编码体系随系统演进叠加产生，分四类动机：

| 动机 | 编码 | 说明 |
|---|---|---|
| **记忆存储** | 40D / global_uid / DNA根码 | 记忆如何编、如何唯一标识、如何聚合 |
| **身份管理** | 实体 UUID / global_uid | 角色与记忆的归属关系 |
| **导航/路径** | locus_path / leaf_zone / branch_id | M1 四层（L0-L3）编码即路径 |
| **标记/标签** | 情绪色号 / narrative_tag / time_period | 附加认知标签（本体-标签分离原则） |
| **历史演进** | 24D / 32D / perception_v2 / VAD | 各阶段维度方案，部分已根除/废弃 |

---

## 二、编码全景表（按状态分级）

### ✅ A 级：核心编码 — 保留且运行良好

| # | 编码 | 字段 | 格式 | 生成者 | 消费方 | 填充率 |
|---|---|---|---|---|---|---|
| A1 | **40D 感知向量** | `perception_40d` | `{__v:2, dims:[40]}` | M3 语义维 + 瑶光客观/瑶灵主观回填（天权中继） | 记忆检索、感知注入、黑钻 40D | **100%** |
| A2 | **实体 UUID** | `entities.uuid` | `TXS-000000NNN` | 实体注册（FamilyGraph） | 全链路实体归属、记忆标注 | 100% |
| A3 | **情绪标签** | `primary_emotion` | 色号 `#E74C3C` + 词库 | M1 情绪编码（emotion_lexicon.json） | 情绪检索、叙事 | 98% |
| A4 | **记忆唯一键** | `global_uid` | `MM` + 8位hash | M1 generateId / 启动修复回填 | 感知回填关联、原子修复索引（state_spines） | 61%（主记忆 0 缺失，锚点设计无） |

### 🟡 B 级：有真实用途但链路断裂 — 需修复

| # | 编码 | 字段 | 格式 | 真实用途（代码证据） | 断点 | 修复方案 |
|---|---|---|---|---|---|---|
| B1 | **DNA 根码** | `dna_root_id` | `6位序号+14位时间码+M01+4位L0码` | ①对话组聚合键（`根码_DG_xxx`）②thread 归组（MemoryAssessor）③note/fact 归组（YuyaoMemoryService） | 主记忆落库 fallback 到记忆 id（0.3%，10/3767） | DNA 接口补 `dna_root_id` 字段 + persistence-stage 透传 |
| B2 | **L3 实体基因** | `entity_genes` | JSON `[{name,type,allele,phenotype,knowledge_type}]` | ①实体归属解析（EntityOwnershipResolver）②知识上下文过滤（KnowledgeContextBuilder） | 8-06 后断写（7%，269 条全为旧记忆） | L3 标注结果随 writeMemory 落库 |
| B3 | **区位指纹** | `location_fingerprint` | `scene_type:scene_id:sub_zone` | G2 时空一致性闸门（FiveStageGate，防跨场景误召回）+ 瑶光场景定位 | 54% 缺失 | 对话时确保指纹生成并回填 |

### 🟠 C 级：未启用 — 决策记录

| # | 编码 | 字段 | 设计用途 | 当前状态 | 决策 |
|---|---|---|---|---|---|
| C1 | **VAD 情绪三维** | `vad_spectrum` | Bionic 歌单谱曲（Valence/Arousal/Dominance） | 依赖外部 Bionic 服务（:7200/:8100），**服务未启动** → 短路降级 → 0 条；无下游消费方 | **暂不启用（2026-08-24 决策 B）**：字段保留、标注可激活，不进修复范围 |
| C2 | **32D 映射表** | —（死代码） | Heart 24D↔YaoLing 32D 映射 | 24D 已根除、瑶灵已 40 通道 | **废弃**：`dim_mapping_24d_32d.ts` / `Dim24to32Migration.ts` 标记废弃 |

### ⚫ D 级：无使用点 — 建议废弃清理

| # | 编码 | 字段 | 证据 | 处置 |
|---|---|---|---|---|
| D1 | **DNA 全码** | `dna_full_code` | 无读取点；黑钻 81 条全 0，晋升实际用 `source_id/emotion_vector/l2_norm` | 删列或保留空置并标注废弃 |
| D2 | **叙事嵌入** | `narrative_embedding` | 无写入代码、无读取代码 | 删列 |
| D3 | **24D 残留** | `perception_v2`（100条）+ `perception_json`（已删列） | 24D 已根除（V12.4） | 清残留数据 + 归档历史脚本（BackfillDualHelix） |
| D4 | **旧占位根码** | `TT00000001M01SYS0000000` | fallback 占位（瑶灵侧已兼容真实格式） | 清理占位路径 |

### 🟢 辅助编码（运行中，归入 A 类管理）

| 编码 | 字段 | 说明 | 状态 |
|---|---|---|---|
| 对话组 ID | `dialog_group_id` | `根码_DG_序号`，对话轮次聚合 | ✅ 运行 |
| 分支路由码 | `branch_id` | `evt_YYYYMMDD_NNN`（L1） | ✅ 运行 |
| 导航路径 | `locus_path` / `leaf_zone` | M1 L0/L2 编码即路径 | ✅ 运行 |
| 叙事时间标记 | `time_period` / `season` / `lunar_term` | 叙事时间锚定 | ✅ 61% |
| 叙事标签 | `narrative_tag` | 叙事分类 | 🟡 3%（低频，按需） |
| 情感次标签 | `secondary_emotions` | 次要情绪 | 🟡 3 条（低频） |

---

## 三、编码生命周期规约

每个编码必须遵循「**本体-标签分离**」与「**生成者-消费方契约**」：

```
生成者（写入） → 存储字段 → 消费方（读取干活）
   必须明确         唯一        必须有实际消费方，否则废弃
```

- **本体编码**（决定记忆身份/路径）：40D、UUID、global_uid、DNA 根码、locus_path、branch_id —— 不可删减
- **标签编码**（附加认知）：情绪色号、narrative_tag、time_period —— 可增删不影响身份
- **死字段判定**：连续无消费方 → 标注废弃 → 下个迁移窗口删列

---

## 四、执行路线图

| 优先级 | 任务 | 涉及 | 说明 |
|---|---|---|---|
| **P0-1** | 修 DNA 根码透传 | wenstar-cc（豁免+流水线） | DNA 接口补 `dna_root_id` 字段，persistence-stage 透传根码到 memories |
| **P0-2** | 修 entity_genes 落库 | wenstar-cc | L3 标注结果随 writeMemory 落库，恢复实体归属精度 |
| **P1-1** | location_fingerprint 回填完善 | wenstar-cc + 瑶光 | 对话时确保指纹生成回填，G2 闸门恢复完整 |
| **P2-1** | 废弃 DNA 全码/叙事嵌入 | wenstar-cc 迁移 | 删列或标注废弃 |
| **P2-2** | 清 24D 残留 + 旧占位根码 | wenstar-cc 数据 | 清 perception_v2、清理 TT 占位 |
| **P2-3** | 标记 32D 死代码废弃 | wenstar-cc | dim_mapping_24d_32d 等 |
| **P3-1** | VAD 字段标注「可激活」 | 文档 | 决策 B 记录（2026-08-24） |

---

## 五、VAD 决策记录（2026-08-24）

- **背景**：vad_spectrum 仅 Bionic 歌单谱曲消费；Bionic 服务（:7200/:8100）未启动，无下游读取。
- **决策**：**暂不启用（B）**。字段保留、标注「可激活」；bionic-brain 代码完整，未来启用只需启动服务。
- **影响**：不进入 P0/P1/P2 修复范围；歌单功能当前不可用属预期。

---

## 六、变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-24 | V1.0 | 初始规约：基于全量审计，确立 A/B/C/D 分级与执行路线图 |
