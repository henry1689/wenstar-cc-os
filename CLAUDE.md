# 文曲星 · 玉瑶 · 太虚境

## 🔴 永久行为规则（自动执行，无需调用技能）

以下规则在每次对话中自动生效，不需要你主动调用任何命令：

### 1. 改代码前 → 自动影响面分析
在修改任何代码文件之前，我必须先说明：
- 改什么文件、为什么改
- 会影响哪些模块/API
- 改完怎么验证

### 2. 改完代码 → 自动出变更报告
任何代码变更完成后，自动输出四段式报告：
```
🔍 变更概述
📁 影响文件
✅ 验证结果
⚠️ 注意事项
```

### 3. "完成了" → 必须有证据
每次我说"完成了"或"修好了"，必须附带：
- 编译检查结果（命令输出）
- 测试结果（命令输出）
- 服务状态（如适用）

### 4. 测试对话 → 主动提醒清理
如果用测试模式产生了测试数据，结束时主动问："要不要清理测试数据？"

### 5. 🔴 禁止打补丁，只做架构性修复
阶段定义：
- **概念阶段**：快速验证可行性，可以临时补丁
- **精调阶段（当前）**：已经过了概念验证，**任何人（包括我）不允许用条件分支/补丁来解决问题**

### 6. 🔴 代码变更后 → 五道核验 + 行为核验 + 改善报告 + 上传确认

任何 `.ts`/`.js` 文件被修改后，按以下流程走完：

```
修改代码
  ↓
① 五道机械核验（编译·功能·牵连·服务·监控）
  ↓  有一道不过 → 回退→诊断→修→重验
  ↓  五道全过 ↓
② 第六道行为核验 ★必做★
  发测试消息验证 LLM 回复不出现以下问题：
  - 角色称呼泄漏(鸿艺叔/梓铭/爸爸/叔叔)
  - 身份混淆(说自己是诗韵/诗雨等角色)
  - 场景循环(浴缸等旧场景)
  - 重复回复("你再说一遍好不好")  
  ↓  行为不过 → 回退→诊断→修→重验
  ↓  行为通过 ↓
③ 提交（本地 git commit）
  ↓
④ 输出改善报告（根因·改了什么·核验证据·注意事项）
  ↓
⑤ 问用户："是否上传同步？"
  ↓
⑥ 用户确认后才 git push
```

**铁律**：
- 永远不自动推送。每次推送前必须等用户确认。
- 改善报告必须有核查证据，不是"应该没问题"。
- 行为核验只测机械核验过不了的——**编译通过≠聊天正常**。
  
判定标准：
| 补丁行为 | 架构修复 |
|---------|---------|
| `if (这个场景) 注入这个` | 改为数据驱动，无条件分支 |
| 在现有函数中加一个 else 分支 | 重新设计函数使其不需要条件判断 |
| 复制一段代码改几个参数 | 提取参数化函数 |
| 在调用链中间加一层检查 | 重新设计调用链使其自然覆盖 |

**铁律**：每遇到一个新场景暴露出的问题，不是往现有代码加一个 if，而是改设计让这个 if 变得不必要。

---

## 🔴 代码质量铁律（基于 2026-07-02 全量扫描）

以下规则是代码审查红线，违反的 PR 不通过。

### P0 致命级 — 必须第一时间清零

| # | 铁律 | 禁止行为 | 替代方案 | 验收标准 |
|:-:|------|---------|---------|---------|
| 1 | **禁止空 catch** | `catch {}` 或 `catch (_) {}` | 核心链路（记忆写入/DB/备份/定时任务）：`catch(e){console.error(...); hook.alert()}`；非核心：至少 `console.error('[模块]', e?.message)`。**确实需静默的必须加注释 + logger.debug** | ①全局搜索 `catch\s*\{\s*\}` 确认归零；②故意构造DB错误/API密钥错误/权限错误，验证日志输出模块名+错误信息+堆栈 |
| 2 | **`bus.on` 必须有 `bus.off`** | 注册事件监听不清理 | `destroy()` 中 `this.bus?.off(event, handler)`。热重载时先销毁旧实例再注册新实例 | ①模拟3次模块重载后监听器数不变；②触发一次事件确认回调只执行1次 |
| 3 | **Hook 数据必须真实，禁止伪造** | 强制 setStatus green、批量伪造 callCount | 移除10s保活循环 + 移除聊天全勾选伪造。超时3min→灰色，超时10min→红色 | ①确认无强制保绿/伪造计数代码；②注释埋点后3min变灰/红；③10轮对话后计数=10 |

### P1 重要级 — 分批清理

| # | 铁律 | 禁止行为 | 替代方案 |
|:-:|------|---------|---------|
| 4 | **禁止模块级 process.env** | 模块顶层读环境变量 | `ConfigService.get('KEY')` 运行时懒加载 |
| 5 | **禁止滥用 `as any`** | 新增 `as any` 类型逃逸 | 定义正确类型或用 `as` 具体类型断言。日常迭代哪个模块碰了就顺手补上 |
| 6 | **写入路径必须走公共 API** | `(sqlite as any).xxx()` 操作原始 sql.js | `SQLiteAdapter.writeMemory()` / `FusionStorageAdapter` |
| 7 | **角色扮演规则必须集中** | chat.ts 内联构建规则 | `buildRoleplayRules()` |

### P2 整洁级 — 顺手处理

| # | 建议 |
|:-:|------|
| 8 | 大文件拆分（chat.ts/server.ts/FamilyGraph.ts/SQLiteAdapter.ts）等P0+P1稳定运行1周后分批拆，拆一块验一块 |
| 9 | 重复字符串提取常量 + `@ts-ignore` → `@ts-expect-error` + 旧文件清理 |

## 🛡️ 整改执行四铁则

```
1. 批次推进，绝不并行 — P0→P1→P2，每批验收通过+稳定2天再进下一批
2. 只修问题，不顺手优化 — 改catch就只改catch，try里面一行都不动
3. 改一项，验一项 — 每类问题构造对应场景验证后方可进入下一项
4. 全程可回滚 — 每批次开独立分支，验证不通过立刻回滚
```

## 🛡️ 质量改进流程

每次修复必须经过四步：

```
① 怎么改 → 根因分析(5 Why) + 影响面 + 改法选择 + 写测试
② 防复发 → 代码门禁 + 运行时监控 + 定期体检 + 开发规范
③ 验证 → 场景验证(故意构造错误) + 烟雾测试 + 灰度观察2天
④ 监控 → 错误日志告警(10min>5条) + 事件监听器巡检(每小时) + 探针健康度巡检(5分钟)
```

详见 `docs/quality-improvement-workflow.md`

## 🎯 预置目标指令

| 命令 | 效果 |
|:----|------|
| `/goal` | 执行角色扮演域第一阶段：独立域框架 + 五步核心管线 + chat.ts 迁出 |

详见 `docs/roleplay-pipeline-redesign.md`

## 📋 需要你主动调用的技能

| 场景 | 命令 |
|------|------|
| 想法模糊，需要推演方案 | `/brainstorm` |
| 方案确定，需要拆解步骤 | `/write-plan` 然后 `/execute-plan` |
| 出 bug 了 | `/debug` |
| 代码写完了，审一遍 | `/review-code` |
| 想并行开发 | `/subagent-dev` |
| 要收尾提交 | `/finish-branch` |
| 安全检查 | `/security-audit` |
| 安全重构 | `/refactor` |
